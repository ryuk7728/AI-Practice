# 0009 PyTorch Tensor Basics

## Problem

Create a function that demonstrates common PyTorch tensor operations.

```python
def tensor_basics(device=None):
```

The function should return a dictionary containing:

- `tensor`: a tensor created from the nested values `[[1, 2, 3], [4, 5, 6]]`
- `from_numpy`: a tensor created from a NumPy array containing the same values
- `sum_last_axis`: a sum over the last axis of `from_numpy` while preserving dimensions
- `random`: random values with shape `(3, 2)` sampled from the range `[0, 1)`
- `shape`: shape of `from_numpy`
- `dtype`: dtype of `from_numpy`
- `ndim`: number of dimensions in `from_numpy`
- `device_before_transfer`: device of `from_numpy` before transfer
- `transferred`: `from_numpy` transferred to the requested device
- `numpy`: `transferred` converted back to a NumPy array after moving to CPU if needed
- `float_tensor`: `from_numpy` converted to floating point dtype

If `device` is not provided, use `"cuda"` when CUDA is available, otherwise `"cpu"`.

## Learning Objectives

- Create PyTorch tensors.
- Convert between NumPy arrays and tensors.
- Inspect tensor metadata.
- Move tensors between devices.
- Convert tensor dtype.

## Examples

```python
results = tensor_basics()
```

The returned dictionary should expose the listed tensor values and metadata.

## Complexity Discussion

For the fixed tensors used in this exercise:

- Time complexity: `O(1)`
- Space complexity: `O(1)`

## Files

- Starter code: `solutions/q0009.py`
- Tests: `tests/test_0009.py`
